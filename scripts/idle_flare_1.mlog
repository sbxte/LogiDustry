# global
set distance 30
set utype @flare

sensor ox @this @x
sensor oy @this @y

_loop:
ubind utype
sensor flag @unit @flag
jump _loop notEqual flag 0 # Ignore if flag isnt 0
op rand angle 360 0
op sin x angle 0
op cos y angle 0
op mul x x distance
op mul y y distance
op add x x ox
op add y y oy
ucontrol move x y
